const addCounter = (list) => {

}

const testAddCounter = () => {
  const listBefore = []
  const listAfter = [0, 1, 2]
  console.log(
    addCounter(listBefore) === listAfter
  )
}

testAddCounter()
